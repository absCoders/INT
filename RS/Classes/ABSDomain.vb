Imports System.Net.Sockets

Public Class ABSDomain
    Inherits MarshalByRefObject

    Public Sub New()

    End Sub

    Public Sub Start(client As SocketInformation)
        'Dim ABSControl = New ABSControl.ABSControl(client)
        'ABSControl.Start()

    End Sub
End Class